const { osu_db_load } = require('osu-tools');

module.exports = async (sended_messages) => {
    //sended_beatmaps.map( x => x.message_id );

    //const osu_db_results = osu_db_load(osu_db_path, beatmap_props);
   // const beatmaps = osu_db_results.beatmaps;

    


}